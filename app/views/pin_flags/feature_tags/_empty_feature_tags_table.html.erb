<%# locals: (search_param: nil) %>

<div class="has-text-centered mt-6">
  <p class="has-text-weight-semibold is-size-4">
    <%= "No feature tags #{search_param.present? ? 'found' : 'created yet'}." %>
  </p>
</div>